import {morphemeParser} from './morphemeParser';

const txtFiles = ['your.txt', 'crawled.txt', 'texts.txt']; //

const countKeyword = () => {
  // write your counting keyword code
  // blah blah...
};


for(const txtFile of txtFiles) morphemeParser(txtFile, countKeyword);
